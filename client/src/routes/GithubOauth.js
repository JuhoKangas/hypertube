const Oauth = () => {
// useffect will make a call to the backend with code from url (searchParams)
// POST https://github.com/login/oauth/access_token to teh backend 
// github will send access token that I will use to get info about the user --> use seperate request for getting user info
// return loader
}

export default Oauth